# Environment variables for RS Creations Tech Digital Hub
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control

# Supabase Configuration (get from https://supabase.com)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here

# Notes:
# - VITE_ prefix means these variables are exposed to the frontend (safe)
# - Only use the ANON (publishable) key, NEVER the service role key
# - Get keys from Supabase Dashboard > Project Settings > API
